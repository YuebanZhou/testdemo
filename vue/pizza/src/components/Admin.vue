<template>
  <h1>Admin</h1>
</template>

<script>
export default {
  /*data() {
    return {
      name: "yueban"
    };
  },*/
  /*beforeRouteEnter: (to, from, next) => {
    //alert("Hello " + this.name); //这里获取不到name，输出内容是Hello undefined
    next(vm => {
      alert("Hello " + vm.name);
    });
  },*/
  /*beforeRouteLeave: (to, from, next) => {
    if(confirm("确定离开么") == true) {
      next()
    }else {
      next(false)
    }
  },*/
};
</script>
